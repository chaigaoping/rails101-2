<%= form_for @group do |f| %>

<div class="form-group">
    <div class="form-group string optional">
        <%= f.label "title", :class => "string opitonal control-label" %>
        <%= f.text_field :title, :class => "string opin form-control formo-control" %>
    </div>

    <div class="form-group string opitonal">
        <%= f.label "description", :class => "string optional control-label" %>
        <%= f.text_area :description, :class => "string optional form-control" %>
    </div>
</div>
<%= f.submit "Submit", class: "btn btn-primary", data: { disable_with: "Submiting..."} %>
<% end %>
